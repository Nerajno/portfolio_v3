[build]
  command = "astro build --remote"
  publish = "dist"

[build.environment]
  NPM_VERSION = "9.8.1"
  # NODE_VERSION = "20"

[functions]
  node_bundler = "esbuild"

[dev]
  framework = "#custom"
  command = "astro dev"
  port = 3000
  targetPort = 3000


