[build]
  command = "astro build --remote"
  publish = "dist"

[build.environment]
  NPM_VERSION = "10.9.2"
  NODE_VERSION = "22"

[functions]
  node_bundler = "esbuild"

[dev]
  framework = "#custom"
  command = "astro dev"
  port = 3000
  targetPort = 3000


